<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="UserProject.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:UserProject"
    xmlns:views="clr-namespace:UserProject.Views"
    Title="UserProject">
    <!-- xmlns:views="clr-namespace:UserProject.Views"  - > Ruta para direccin de vistas-->

    <ShellContent
        Title="Usuarios"
        ContentTemplate="{DataTemplate views:MainPage}"
        Route="MainPage" />

</Shell>
